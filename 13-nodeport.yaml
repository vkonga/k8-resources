apiVersion: v1
kind: Service
metadata:
  name: Nodeport
  labels:
    purpose: nodeport-demo
spec:
  type: NodePort
  selector:
    purpose: nodeport-demo
    project: electronic-shop
    environment: env
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

---

apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    purpose: nodeport-demo
    project: electronic-shop
    environment: env
spec:
  
  containers:
    - name: nginx
      image: nginx
