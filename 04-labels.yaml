apiVersion: v1
kind: Pod
metadata:
  name: labels
  labels:
    project: electronic-shop
    component: mysql
    tier: backend
    environment: env
spec:
  containers:
    - name: nginx
      image: nginx
    